# Report Save Convention

When saving a loop report, prefer:

- `reports/code-loop/<repo-name>-<task-slug>.md`
- `reports/code-loop/<YYYY-MM>/<repo-name>-<task-slug>.md`

The report should contain:
- task
- repo path
- validation command
- iteration log
- final status
- remaining issues
