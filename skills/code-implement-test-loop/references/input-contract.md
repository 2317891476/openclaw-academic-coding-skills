# Input Contract

Collect these before running the loop:

- task
- repo path
- validation command
- max iterations
- whether dependency installation is allowed
- whether test edits are allowed

If validation command is missing, ask for one unless there is an obvious standard command in the repo.
