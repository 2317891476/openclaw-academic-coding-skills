# Call Template

Recommended invocation:

```text
严格使用 coding-fix-pipeline 处理下面任务，并按该 skill 的默认输出格式返回结果：<你的代码任务>
```

Example:

```text
严格使用 coding-fix-pipeline 处理下面任务，并按该 skill 的默认输出格式返回结果：分析并修复 openclaw-qq 仓库中的一个 bug，输出 change plan、validation loop 和 markdown 报告。
```
